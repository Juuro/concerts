# AMAC Code Review: PR #237 — chore: update vitest configuration to include test files and modify mock for helpers

**Date:** 2026-04-01  
**Author:** Juuro (Sebastian Engel)  
**Branch:** feature/unit-tests-concerts-lib-from-testing-framework → feature/unit-testing-framework  
**Changes:** +4540 -1139 across 9 files  
**CI Status:** pending (GitHub check `claude-review` was in progress at review time; GitGuardian and Vercel succeeded)

## Merge Recommendation: APPROVE WITH COMMENTS — minor issues noted

No CRITICAL or HIGH findings. Address MEDIUM items when convenient (especially `.claude/settings.local.json` cleanup if that file stays in the branch).

## Findings

### CRITICAL (0)

None.

### HIGH (0)

None.

### MEDIUM (4)

1. **[security-specialist]** `.claude/settings.local.json:47` (`permissions.allow`)  
   **Code:** `"Bash(do)"`, `"Bash(if [ ! -f \"$f\" ])"`, `"Bash(then)"`, … `"Bash(done)"`, `"Bash(test:*)"`, incomplete `for file in …`  
   **Issue:** Orphan shell fragments from a decomposed loop clutter the allow list and obscure real permissions.  
   **Evidence:** Confirmed in workspace file at lines 47–57.  
   **Suggestion:** Remove junk entries; use one explicit allow rule if a script needs approval.  
   **Verified:** yes  

2. **[architect]** `src/lib/concerts.test.ts:1` (file scope)  
   **Issue:** Entire concerts test suite in one very large file (~3400 LOC).  
   **Suggestion:** Split by concern (CRUD, pagination, statistics, fork logic) or a `__tests__/concerts/` layout.  
   **Verified:** skipped (MEDIUM)

3. **[quality-engineer]** `vitest.config.ts:20` (`defineConfig`)  
   **Issue:** `**/*.test.*` added to coverage exclude vs PR title wording “include test files” — ambiguous for reviewers.  
   **Suggestion:** Clarify in PR body: excluding test sources from coverage metrics is intentional.  
   **Verified:** skipped (MEDIUM)

4. **[quality-engineer]** `src/lib/concerts.test.ts:49` (`vi.mock`)  
   **Issue:** File-level `@/lib/prisma` mock overlaps global mock in `vitest.setup.ts` with a different shape — drift risk.  
   **Suggestion:** Share one mock factory or extend setup mock.  
   **Verified:** skipped (MEDIUM)

**Grouped root cause:** Item 4 and architect item both relate to **test maintainability / single source of truth for Prisma mocks** — consider one shared factory used by setup + lib tests.

### LOW (2)

1. **[pragmatist]** `TESTING.md` — long duplicated TypeScript examples; prefer linking canonical tests as source of truth. (Verified: skipped)

2. **[resilience-performance]** `vitest.setup.ts` — `$transaction` mock is simplified; extend if batch semantics become important. (Verified: skipped)

### Pre-Existing (0)

None identified in verification pass.

### Positive notes (GDPR)

**[gdpr-compliance]** `TESTING.md` clarifies synthetic public band/venue names vs user-shaped PII; documents caller responsibility for privacy flags. No new data-processing paths in application code.

## Review Stats

- Specialists: 8 (+ Tech Lead aggregation)
- Raw findings: 7 across agents (after dedup/grouping: 4 MEDIUM + 2 LOW + 1 informational)
- Verified: 1 MEDIUM confirmed on `.claude/settings.local.json`; 0 CRITICAL/HIGH to verify
- False positives filtered: 0
- **No issues found by:** api-contract-migration (no contract changes), frontend-specialist (not applicable)

## Local verification

- `yarn test`: **115 tests passed** (5 files) on review workspace.
